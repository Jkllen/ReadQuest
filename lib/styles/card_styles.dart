import 'package:flutter/material.dart';

class CardStyles {
  static final BoxDecoration whiteCard = BoxDecoration(
    color: Colors.white,
    borderRadius: BorderRadius.circular(20),
    border: Border.all(color: const Color(0xFFE2E2E2), width: 1),
  );
}
